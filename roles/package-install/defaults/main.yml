---
# Default variables for package-install role

# Package lists are defined in vars/Debian.yml and vars/RedHat.yml
# They will be loaded automatically based on ansible_os_family

# Control which package sets to install
install_cli_packages: yes
install_desktop_packages: yes

# Package installation options
package_state: present
update_cache: yes
cache_valid_time: 3600