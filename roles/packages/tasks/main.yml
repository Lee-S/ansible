---
- name: include vars
  include_vars: ../vars/main.yml

- name: Install or remove apt packages on Debian or Ubuntu
  block:
    - name: Install apt packages
      apt:
        name: "{{ item }}"
        state: present
      loop: "{{ apt.install }}"

    # - name: Remove apt packages
    #   apt:
    #     name: "{{ item }}"
    #     state: absent
    #   loop: "{{ apt.remove }}"
    #   when: apt.remove is defined
  become: true
  tags: packages

- name: Install laptop-specific packages
  block:
    - name: Install laptop apt packages
      apt:
        name: "{{ item }}"
        state: present
      loop: "{{ apt.install_laptop }}"
      when: ansible_facts['product_version'] is search('ThinkPad')
  become: true
  tags: packages laptop

- name: Disable fingerprint for sudo
  lineinfile:
    path: /etc/pam.d/common-auth
    line: "auth sufficient pam_unix.so try_first_pass likeauth nullok"
    state: present
    insertbefore: BOF
  become: true
  tags: fingerprint

# - name: Enable boot logging on Ubuntu
#   lineinfile:
#     path: /etc/default/grub
#     regexp: '^GRUB_CMDLINE_LINUX_DEFAULT='
#     line: 'GRUB_CMDLINE_LINUX_DEFAULT="bootlogd=1"'
#   notify: Update grub
#   become: true
#   tags: boot_logging

# - name: Update grub configuration
#   command: update-grub
#   become: true
#   tags: boot_logging

