# ansible

### Install prerequisites
```
ansible-galaxy role install diodonfrost.jetbrains_toolbox
```


### To enable fingerprints turn in in PAM
`sudo pam-auth-update`

### Enrol fingerprints or use GNOME Users
`fprintd-enroll`

